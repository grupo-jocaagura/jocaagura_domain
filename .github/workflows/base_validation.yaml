name: Base validation

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - develop
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  base_validation:
    uses: ./.github/workflows/validate_commits_and_lints.yaml